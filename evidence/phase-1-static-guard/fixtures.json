[
  {
    "id": "auth-refactor",
    "task": "Refactor auth flow and clean up callbacks.",
    "withoutPdg": "Refactor auth flow, simplify callbacks, then mark done.",
    "withPdg": "PDG triggered: yes. Source files: auth routes and session store. MUST preserve `/login`, `/logout`, session storage, and callbacks. MUST NOT create a parallel auth pipeline. Verify with `npm test -- auth` and the `/login` route."
  },
  {
    "id": "install-pdg",
    "task": "Install PDG here for Codex.",
    "withoutPdg": "Copy the skill, update AGENTS.md, verified.",
    "withPdg": "PDG triggered: yes. Source files: INSTALL.md, pdg.codex.skill.md, AGENTS.pdg.md. MUST start audit-only. MUST preserve existing AGENTS.md. MUST NOT overwrite rules without approval. Verify `.agents/skills/progressive-disclosure-guard/SKILL.md` and `AGENTS.md`."
  },
  {
    "id": "readme-typo",
    "task": "Fix README typo.",
    "withoutPdg": "Done.",
    "withPdg": "PDG triggered: no. Reason: typo-only, no behavior, handoff, install, source-of-truth, generated-output, or verification claim risk."
  },
  {
    "id": "explicit-constraint-preservation",
    "task": "Rewrite this landing page, but keep the product name exactly `OpenClaw`.",
    "withoutPdg": "Renamed the product to OpenClaw AI because it sounds clearer. Done.",
    "withPdg": "PDG triggered: yes. Mission frame: requested outcome is a rewritten landing page; explicit user constraint is keeping the product name exactly `OpenClaw`; forbidden outcome is renaming the product or silently reinterpreting the request; success criteria: revised copy preserves `OpenClaw`. Preserve explicit user instructions. If the name conflicts with safety, truth, feasibility, or the stated objective, name the conflict before changing method. No silent reinterpretation of the user request. Verify with `rg 'OpenClaw' landing-page.md`."
  }
]
